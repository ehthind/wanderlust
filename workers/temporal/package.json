{
  "name": "@wanderlust/temporal-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node ../../tools/dev/guard-secrets.mjs -- tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json --outDir dist && node ./scripts/postbuild.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config ./vitest.config.ts"
  },
  "dependencies": {
    "@sentry/node": "10.48.0",
    "@temporalio/activity": "^1.15.0",
    "@temporalio/client": "^1.15.0",
    "@temporalio/worker": "^1.15.0",
    "@temporalio/workflow": "^1.15.0"
  }
}
